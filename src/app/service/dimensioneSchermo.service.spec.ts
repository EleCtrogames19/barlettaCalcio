/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DimensioneSchermoService } from './dimensioneSchermo.service';

describe('Service: DimensioneSchermo', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DimensioneSchermoService]
    });
  });

  it('should ...', inject([DimensioneSchermoService], (service: DimensioneSchermoService) => {
    expect(service).toBeTruthy();
  }));
});
